With the use of the youtube API
+ jquery for the video player

In the Terminal
run - bundle install
- rails s

Then you will have a youtube streaming videotelevision
curated by the user on the show page

uses gem figaro to hide youtube api key
+ gem devise so user can select content

